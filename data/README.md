# Data

No datasets are included. raw/ and processed/ are ignored local work areas.
For each public, synthetic or physical source document provenance, license,
download/generation method, checksum/version, feature meanings, preprocessing,
partitions and limitations. Compatibility must be checked before combining data.

Keep ground-truth scenario labels separate from model inputs. Maintain backups
of research data separately from Git.
